spring.application.name=backend

spring.data.mongodb.uri=mongodb://localhost:27017/donation-backend
spring.data.mongodb.database=donation-backend
spring.data.mongodb.auto-index-creation=true

server.port=8080
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
cloudinary.url=cloudinary://718897153392226:ls_48tomsmCS46hSXExaP5h3mhM@dcnthqukd
cloudinary.cloud-name=dcnthqukd
cloudinary.api-key=718897153392226
cloudinary.api-secret=ls_48tomsmCS46hSXExaP5h3mhM

logging.level.org.springframework.security=DEBUG
logging.level.com.crowdfund.backend.config=DEBUG

# Razorpay Keys
razorpay.mode=${RAZORPAY_MODE:test}
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SGSGChkzIpBp8i}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:Tii2A1FP4uNdUitCygC8l89K}
logging.level.com.crowdfund.backend.service.RazorpayOrderService=DEBUG

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Mail configuration for receipt emails
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Gemini Chatbot configuration
gemini.api.key=${GEMINI_API_KEY:}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent}
gemini.api.fallback-url=${GEMINI_API_FALLBACK_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent}
chatbot.system-prompt=You are a helpful assistant for a donation and crowdfunding platform. Answer clearly and keep responses focused on campaigns, donations, NGOs, and payment support.
logging.level.com.crowdfund.backend.controller.ChatbotController=DEBUG
logging.level.com.crowdfund.backend.service.ChatbotService=DEBUG

# JWT
app.jwt.secret=${JWT_SECRET:replace-this-with-a-long-random-secret}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# OTP
otp.expiry-minutes=${OTP_EXPIRY_MINUTES:5}
otp.resend-min-seconds=${OTP_RESEND_MIN_SECONDS:30}
otp.max-send-per-hour=${OTP_MAX_SEND_PER_HOUR:5}
otp.max-verify-attempts=${OTP_MAX_VERIFY_ATTEMPTS:5}

# Twilio SMS
twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.phone-number=${TWILIO_PHONE_NUMBER:}
